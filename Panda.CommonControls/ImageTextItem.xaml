<UserControl x:Class="Panda.CommonControls.ImageTextItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Panda.CommonControls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             PreviewMouseUp="ImageTextItem_OnPreviewMouseUp">
    <UserControl.Resources>
        <ResourceDictionary
            Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.TextBox.xaml" />
    </UserControl.Resources>
    <StackPanel x:Name="LayoutRoot" Orientation="Horizontal" d:DataContext="{d:DesignInstance local:ImageTextItem}">
        <Image Source="{Binding ImageSource}" />
        <StackPanel>
            <Label x:Name="HeaderTextBox" Content="{Binding HeaderText}" />
            <Label x:Name="SubHeaderTextBox" Content="{Binding SubHeaderText}" />
        </StackPanel>
    </StackPanel>
</UserControl>